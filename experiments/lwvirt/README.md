From the source directory of Fixpoint
```
./experiments/lwvirt/run.sh
```

The reported numbers is the end-to-end time of repeating each kind of `add` function 4096 times, and we reported the average time per invocation in the Evaluation section.
